export enum TicketStatus {
  DRAFT = "DRAFT",
  PUBLISHED = "PUBLISHED",
  ARCHIVED = "ARCHIVED",
}

export enum SessionMode {
  EXAM = "EXAM",
  PRACTICE = "PRACTICE",
}

export enum Lang {
  RU = "ru",
  UK = "uk",
}

export enum Difficulty {
  EASY = "EASY",
  MEDIUM = "MEDIUM",
  HARD = "HARD",
}

export enum TicketImageRole {
  PRIMARY = "PRIMARY",
  SECONDARY = "SECONDARY",
}
